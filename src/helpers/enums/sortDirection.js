/* istanbul ignore file -- justification: this is simply an enumeration; no testing needed */

const SORT_DIRECTION = {
  ascending: { displayName: 'ascending', abbreviation: 'asc' },
  descending: { displayName: 'descending', abbreviation: 'desc' }
};

export default Object.freeze(SORT_DIRECTION);
