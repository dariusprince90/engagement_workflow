/* istanbul ignore file -- justification: this is simply an enumeration; no testing needed */

const COMPLIANCE_JOB_TYPES = {
  Audit: { id: 1 },
  Tax: { id: 2 }
};

export default Object.freeze(COMPLIANCE_JOB_TYPES);
