/* istanbul ignore file -- justification: this is simply an enumeration; no testing needed */

const INPUT_GROUP_ADD_ON_LOCATIONS = {
  front: { classSuffix: 'prepend' },
  back: { classSuffix: 'append' }
};

export default Object.freeze(INPUT_GROUP_ADD_ON_LOCATIONS);
